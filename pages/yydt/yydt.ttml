<view class="yfxx-container">
	<view class="items">
		<Item tt:if="{{items.length}}" tt:for="{{items}}" data-idx="{{index}}" bindEvent="toDetailPage" style="display:block; margin-bottom:48rpx;"></Item>
		<image tt:if="{{!items.length}}" src="../../assets/imgs/none.png"></image>
		<text tt:if="{{!items.length}}" class="p">暂时没有文章</text>
	</view>
</view>